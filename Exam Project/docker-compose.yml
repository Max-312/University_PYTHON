services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
    environment:
      - DB_HOST=db
      - DB_NAME=Goods
      - DB_USER=Draharmo
      - DB_PASS=Aiwprton31201%
    depends_on:
      - db
